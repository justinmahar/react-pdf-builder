import { Meta } from '@storybook/addon-docs';
import { DocsPDF } from '../../DocsPDF';

import { RectangleShape } from '../../../../components/shapes/RectangleShape';

<Meta title="Documentation/Components/Shapes/RectangleShape" />

<h1>RectangleShape</h1>

## Description

Description of the RectangleShape component.

## Usage

```jsx
import { RectangleShape } from 'react-pdf-builder';
```

Describe how to use the component here.

```jsx
<RectangleShape label="Bomb Diggity!" />
```

### Props

| Prop            | Type                        | Description                                |
| --------------- | --------------------------- | ------------------------------------------ |
| label           | `string`                    | **Required.** Label shown in the component |
| backgroundColor | `string` &#124; `undefined` | Optional. What background color to use     |

## RectangleShape Example

<DocsPDF>
  <RectangleShape className="p-3 border">Testing 123</RectangleShape>
</DocsPDF>

```jsx
import * as React from 'react';
import { RectangleShape, ThemedPage } from 'react-pdf-builder';
import { PDFViewer, Document } from '@react-pdf/renderer';

export function Example() {
  return (
    <PDFViewer style={{ width: 420, height: 600 }}>
      <Document>
        <ThemedPage size="LETTER">
          <RectangleShape className="p-3 border">Testing 123</RectangleShape>
        </ThemedPage>
      </Document>
    </PDFViewer>
  );
}
```
