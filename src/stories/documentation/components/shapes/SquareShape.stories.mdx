import { Meta } from '@storybook/addon-docs';
import { DocsPDF } from '../../DocsPDF';

import { SquareShape } from '../../../../components/shapes/SquareShape';

<Meta title="Documentation/Components/Shapes/SquareShape" />

<h1>SquareShape</h1>

## Description

Description of the SquareShape component.

## Usage

```jsx
import { SquareShape } from 'react-pdf-builder';
```

Describe how to use the component here.

```jsx
<SquareShape label="Bomb Diggity!" />
```

### Props

| Prop            | Type                        | Description                                |
| --------------- | --------------------------- | ------------------------------------------ |
| label           | `string`                    | **Required.** Label shown in the component |
| backgroundColor | `string` &#124; `undefined` | Optional. What background color to use     |

## SquareShape Example

<DocsPDF>
  <SquareShape className="p-3 border">Testing 123</SquareShape>
</DocsPDF>

```jsx
import * as React from 'react';
import { SquareShape, ThemedPage } from 'react-pdf-builder';
import { PDFViewer, Document } from '@react-pdf/renderer';

export function Example() {
  return (
    <PDFViewer style={{ width: 420, height: 600 }}>
      <Document>
        <ThemedPage size="LETTER">
          <SquareShape className="p-3 border">Testing 123</SquareShape>
        </ThemedPage>
      </Document>
    </PDFViewer>
  );
}
```
