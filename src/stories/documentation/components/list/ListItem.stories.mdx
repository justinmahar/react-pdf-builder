import { Meta } from '@storybook/addon-docs';
import { DocsPDF } from '../../DocsPDF';

import { OrderedList } from '../../../../components/lists/OrderedList';
import { ListItem } from '../../../../components/lists/ListItem';

<Meta title="Documentation/Components/Lists/ListItem" />

<h1>ListItem</h1>

## Description

Description of the ListItem component.

## Usage

```jsx
import { ListItem } from 'react-pdf-builder';
```

Describe how to use the component here.

```jsx
<ListItem label="Bomb Diggity!" />
```

### Props

| Prop            | Type                        | Description                                |
| --------------- | --------------------------- | ------------------------------------------ |
| label           | `string`                    | **Required.** Label shown in the component |
| backgroundColor | `string` &#124; `undefined` | Optional. What background color to use     |

## ListItem Example

<DocsPDF>
  <ListItem className="p-3 border">Testing 123</ListItem>
</DocsPDF>

```jsx
import * as React from 'react';
import { ListItem, ThemedPage } from 'react-pdf-builder';
import { PDFViewer, Document } from '@react-pdf/renderer';

export function Example() {
  return (
    <PDFViewer style={{ width: 420, height: 600 }}>
      <Document>
        <ThemedPage size="LETTER">
          <ListItem className="p-3 border">Testing 123</ListItem>
        </ThemedPage>
      </Document>
    </PDFViewer>
  );
}
```
