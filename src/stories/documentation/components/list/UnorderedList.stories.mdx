import { Meta } from '@storybook/addon-docs';
import { DocsPDF } from '../../DocsPDF';

import { UnorderedList } from '../../../../components/lists/UnorderedList';
import { ListItem } from '../../../../components/lists/ListItem';

<Meta title="Documentation/Components/Lists/UnorderedList" />

<h1>UnorderedList</h1>

## Description

Description of the UnorderedList component.

## Usage

```jsx
import { UnorderedList } from 'react-pdf-builder';
```

Describe how to use the component here.

```jsx
<UnorderedList label="Bomb Diggity!" />
```

### Props

| Prop            | Type                        | Description                                |
| --------------- | --------------------------- | ------------------------------------------ |
| label           | `string`                    | **Required.** Label shown in the component |
| backgroundColor | `string` &#124; `undefined` | Optional. What background color to use     |

## UnorderedList Example

<DocsPDF>
  <UnorderedList className="p-3 border">Testing 123</UnorderedList>
</DocsPDF>

```jsx
import * as React from 'react';
import { UnorderedList, ThemedPage } from 'react-pdf-builder';
import { PDFViewer, Document } from '@react-pdf/renderer';

export function Example() {
  return (
    <PDFViewer style={{ width: 420, height: 600 }}>
      <Document>
        <ThemedPage size="LETTER">
          <UnorderedList className="p-3 border">Testing 123</UnorderedList>
        </ThemedPage>
      </Document>
    </PDFViewer>
  );
}
```
