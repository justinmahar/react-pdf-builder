import { Meta } from '@storybook/addon-docs';
import { DocsPDF } from '../../DocsPDF';

import { PDFViewer, Document } from '@react-pdf/renderer';

import { ThemedPage } from '../../../../components/basics/ThemedPage';
import { Paragraph } from '../../../../components/typography/Paragraph';

<Meta title="Documentation/Components/Basics/ThemedPage" />

<h1>ThemedPage</h1>

## Description

A page component that supports class names. Add your PDF contents as children of this component.

> This component is a wrapper for react-pdf [Page](https://react-pdf.org/components#page) that adds swatch and class name support via [themes](https://justinmahar.github.io/react-pdf-builder/?path=/docs/documentation-themes--docs).

## Usage

```jsx
import { ThemedPage } from 'react-pdf-builder';
```

```jsx
<ThemedPage size="LETTER">
  Contents of page go here.
</ThemedPage>
```

### Props

| Inherits all props from                       |
| --------------------------------------------- |
| [Page](https://react-pdf.org/components#page) |

And supports the following props:

| Prop      | Type                        | Description                                                                                                                                                                                       |
| --------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| swatch    | `SwatchColor`               | Optional. One of the [swatch color names](https://justinmahar.github.io/react-pdf-builder/?path=/docs/documentation-themes--docs#swatch-colors) from the theme, as a string.                      |
| className | `string` &#124; `undefined` | Optional. Class name string used to style the component. [Class names](https://justinmahar.github.io/react-pdf-builder/?path=/docs/documentation-themes--docs#class-names) are defined in themes. |

## ThemedPage Example

<PDFViewer style={{ width: 420, height: 600 }}>
  <Document>
    <ThemedPage size="LETTER">
      <Paragraph>This is a themed page. It supports class names from the theme.</Paragraph>
    </ThemedPage>
  </Document>
</PDFViewer>

```jsx
import * as React from 'react';
import { ThemedPage, Paragraph, ThemedPage } from 'react-pdf-builder';
import { PDFViewer, Document } from '@react-pdf/renderer';

export function Example() {
  return (
    <PDFViewer style={{ width: 420, height: 600 }}>
      <Document>
        <ThemedPage size="LETTER">
          <Paragraph>This is a themed page. It supports class names from the theme.</Paragraph>
        </ThemedPage>
      </Document>
    </PDFViewer>
  );
}
```