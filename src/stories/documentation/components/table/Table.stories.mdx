import { Meta } from '@storybook/addon-docs';
import { DocsPDF } from '../../DocsPDF';

import { Table } from '../../../../components/table/Table';
import { TableRow } from '../../../../components/table/TableRow';
import { TableCell } from '../../../../components/table/TableCell';

<Meta title="Documentation/Components/Tables/Table" />

<h1>Table</h1>

## Description

Description of the Table component.

## Usage

```jsx
import { Table } from 'react-pdf-builder';
```

Describe how to use the component here.

```jsx
<Table label="Bomb Diggity!" />
```

### Props

| Prop            | Type                        | Description                                |
| --------------- | --------------------------- | ------------------------------------------ |
| label           | `string`                    | **Required.** Label shown in the component |
| backgroundColor | `string` &#124; `undefined` | Optional. What background color to use     |

## Table Example

<DocsPDF>
  <Table className="p-3 border">Testing 123</Table>
</DocsPDF>

```jsx
import * as React from 'react';
import { Table, ThemedPage } from 'react-pdf-builder';
import { PDFViewer, Document } from '@react-pdf/renderer';

export function Example() {
  return (
    <PDFViewer style={{ width: 420, height: 600 }}>
      <Document>
        <ThemedPage size="LETTER">
          <Table className="p-3 border">Testing 123</Table>
        </ThemedPage>
      </Document>
    </PDFViewer>
  );
}
```
